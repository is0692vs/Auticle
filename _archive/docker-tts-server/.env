# Docker Edge TTS Server Environment Configuration

# サーバー設定
TTS_HOST_PORT=8001
TTS_HOST_IP=0.0.0.0

# 音声合成設定
DEFAULT_VOICE=ja-JP-NanamiNeural
DEFAULT_RATE=+0%

# Docker設定
COMPOSE_PROJECT_NAME=audicle-edge-tts

# LAN アクセス用設定例（必要に応じてコメントアウト解除）
# TTS_HOST_PORT=8002
# 他のPCからアクセスする場合は、そのPCでSSHポートフォワーディング等を使用
# 例: ssh -L 8001:localhost:8001 server-pc